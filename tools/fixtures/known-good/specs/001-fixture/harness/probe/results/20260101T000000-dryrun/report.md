# Probe run — report

> ## DRY RUN — NOT RESULTS
>
> No model was called. Every judge row below comes from a deterministic stub.

## Decision table — VOID, NOT A VERDICT

- MD_best (discounted) = 30.7 pp on arm c2 — CLASSIFICATION WITHHELD, stub judge.
- ~~MD_best clears the gate, so the verifier is a headline feature.~~ (struck 2026-01-01 — wrong, not narrowed.)

> UNDERPOWERED — ADVISORY. A positive result licenses only "consistent with H2, underpowered"; it does not license "H2 confirmed".

The margin over a judge is UNMEASURED. Nothing here clears the gate and nothing here fails it,
because the gate reads a quantity defined over judge verdicts and no judge verdict exists.

Every row of the oracle ledger carries a lowercase `verdict` field, which is a column name and
not a claim; the check matches an upper-case label only, for exactly that reason.

The next line is the far edge of locality. Its disclosure opens the line and its
label closes it, and the two sit exactly as far apart as the proximity bound
reaches — so the line must stay silent, and must start being reported the moment
that bound is narrowed by one character.

dry run, the judge was a hash, and the label ending this line sits exactly as far from this notice as locality reaches — VERDICT: pending a live judge
