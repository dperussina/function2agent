# Tasks: Fixture Feature

**Input**: [`spec.md`](./spec.md) (~~3~~ **4** functional requirements, 3 success
criteria, two user stories)

*(the functional-requirement count was recounted 2026-01-01 and advanced; the
struck figure predates the fourth requirement)*

Four constructs here, each of which has a way of going wrong.

The advanced figure carries emphasis between the number and its noun, which is
how this corpus writes a corrected count. A pattern that expects a bare space
there stops matching and reports nothing — the same blindness `_RANGE` has to
`**` between two identifiers.

The struck figure beside it is history, not a claim. Reading it as live would
make the strike convention unsatisfiable, because the only way to satisfy the
check would be to delete the correction.

The success-criteria count is split across the line break above, with its number
on one line and the word `criteria` on the next. That is ordinary hard-wrapped
prose and it is a real site, so it must be both found and found correct.

And this file mentions two user stories, which no rule counts, and quotes a
count inside a fenced block, which no rule may read:

```
| claimed | 99 functional requirements, 42 success criteria |
```
